float disks[2][4];
int raw, column;

for(raw = 0; raw < 2; raw++)
{
    for(raw = 0; raw < 2; raw++)
    {
        scanf("%f", &disks[raw][column]);
    }
}

// Visualizing the table 
for(raw = 0; raw < 2; raw++)
{
    for(column = 0; column < 4; column++)
    {
        printf("\n Pts %.1f \n", disks [raw][columns]);
    }
}

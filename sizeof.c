#include <stdio.h>
/*Print the size of the floating point and double values */


int main()
{
    printf("The size of floating point variables are %d \n",
	    sizeof(float));
    printf("The size of double precision variables is %d \n",
	    sizeof(double));
    return 0;
}
